import React from 'react';
import './styles.css'; // Make sure to include the corresponding CSS file

const Spinner = () => (
    <div className="spinner-overlay">
        <div className="spinner"></div>
    </div>
);

export default Spinner;
